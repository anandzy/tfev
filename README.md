# tfev
Automatically extracting variables/values from Terraform tfvars and converting/exporting to other formats (for GitLab CI, GitHub Actions, other tfvars, env files, etc.), without needing to manually declare them again.
